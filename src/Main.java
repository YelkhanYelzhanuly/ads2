import java.util.LinkedList;

public class Main {
    public static void main(String[] args) {
        Stack<Integer> node = new Stack<>();
        node.insert(15);
        node.insert(25);
        node.insert(35);
        node.getstack();
    }
}